// Define uma classe chamada Pokemon
class Pokemon {
  // Declaração das propriedades da classe Pokemon
  // Essas propriedades serão usadas para armazenar informações sobre cada Pokemon
  // As propriedades são apenas declaradas, mas não são inicializadas com valores específicos
  // Elas serão inicializadas posteriormente quando um objeto Pokemon for criado
  number; // Armazena o número (ID) do Pokemon
  name; // Armazena o nome do Pokemon
  type; // Armazena o tipo principal do Pokemon
  types = []; // Armazena um array contendo todos os tipos do Pokemon
  photo; // Armazena a URL da foto (imagem) do Pokemon
}
